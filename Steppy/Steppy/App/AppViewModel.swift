struct AppViewModel {
    /* TODO
     - check API accessToken in secure persistence:
        true:
            POST auth to the API
                persist new token
                    present home
        false: present onboarding
    */
}
